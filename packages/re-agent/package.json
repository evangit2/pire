{
	"name": "@earendil-works/pi-re-agent",
	"version": "0.83.0",
	"description": "Reverse engineering agent for pire — native tools, crash recovery, auto-detect, lean TUI",
	"type": "module",
	"main": "src/index.ts",
	"bin": {
		"pire": "src/cli.ts"
	},
	"scripts": {
		"test": "node test/test-suite.cjs && node test/test-models.cjs --quick && node test/test-e2e.cjs",
		"check": "tsc --noEmit"
	},
	"dependencies": {
		"typebox": "*"
	},
	"devDependencies": {
		"tsx": "*",
		"typescript": "*"
	}
}
